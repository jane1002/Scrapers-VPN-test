{
	"name": "LegalProceedings-Scrapers-test-version",
	"version": "0.0.1",
	"description": "This is an example of an Apify actor.",
	"dependencies": {
		"apify": "^0.21.0",
		"dotenv": "^8.2.0",
		"@types/dotenv": "6.1.0",
		"json2csv": "^5.0.1",
		"moment": "^2.27.0",
		"querystring": "^0.2.0"
	},
	"devDependencies": {
		"@types/json2csv": "^5.0.1",
		"@types/moment": "^2.13.0",
		"@types/node": "^14.0.14",
		"@typescript-eslint/eslint-plugin": "^3.5.0",
		"@typescript-eslint/parser": "^3.5.0",
		"eslint": "^7.3.1",
		"husky": "^4.2.5",
		"typescript": "^3.9.6"
	},
	"scripts": {
		"lint": "eslint src --fix --ext .ts",
		"build": "npm run lint && tsc --build",
		"test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1",
		"tx": "npm run build && node dist/src/TX/tx.js",
		"fl": "npm run build && node dist/src/FL/fl.js",
		"ca": "npm run build && node dist/src/CA/ca.js",
		"main": "npm run build && node dist/index.js"
	},
	"husky": {
		"hooks": {
			"pre-commit": "npm run lint"
		}
	},
	"author": "Cerebro",
	"license": "ISC"
}
